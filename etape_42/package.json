{
  "name": "badges",
  "version": "0.0.1",
  "description": "Badges proto",
  "author": "Fred Ménez @ Scalera SAS",
  "license": "MIT",
  "homepage": "",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fredmenez"
  },
  "scripts": {
    "dev": "snowpack dev",
    "build": "npm run lint && npm run less && npm run js",
    "watch": "watch \"npm run less-dev && npm run js-dev\" src --wait 0.1 --interval 0.1",
    "lint": "eslint {examples,src}/**/*.js --max-warnings 0",
    "lintfix": "npm run lint -- --fix"
  },
  "pre-commit": [
    "lint"
  ],
  "devDependencies": {
    "http-proxy": "^1.18.1",
    "less": "^3.11.1",
    "less-plugin-clean-css": "^1.5.1",
    "pre-commit": "^1.2.2",
    "snowpack": "^3.0.11",
    "watch": "^1.0.2"
  },
  "dependencies": {
  }
}
